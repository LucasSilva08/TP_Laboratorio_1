/** \brief Calula la suma de dos operando
 *
 * \param float primer operando
 * \param float segundo operando
 * \return float resultado de la suma
 *
 */
float calcularSuma(float,float);
/** \brief calcula la resta entre dos operando
 *
 * \param float primero operando
 * \param float segundo operando
 * \return float resultado de la resta
 *
 */
float calcularResta(float,float);
/** \brief calcula la multiplicacion entre dos operando
 *
 * \param float primer operando
 * \param float segundo operando
 * \return float resultado de la multiplicacion
 *
 */
float calcularMultiplicacion(float,float);
/** \brief calcula la division entre dos  si el segundo operando es !=0
 *
 * \param float primer operando
 * \param float segundo operando
 * \return float resultado de la division
 *
 */
float calcularDivision(float,float);
/** \brief calcula el factorial del operando
 *
 * \param float operando
 * \return int resultado
 *
 */
int calcularFactorail(float);

